import { cn } from "@lib/utils";

type Props = {
  className?: string;
};

function SettingsPage({ className }: Props) {
  return <div className={cn("", className)}>In progress... Stay tuned!</div>;
}

export default SettingsPage;
